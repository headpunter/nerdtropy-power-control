# setting_changed Event

Fires whenever a setting is modified via the `settings` API.

## Return Values
1. `"setting_changed"` (string)
2. name (string) — the setting identifier
3. newValue (any) — the new value
4. oldValue (any) — the previous value
