# websocket_message Event

Fires when a message arrives on an active WebSocket connection.

## Return Values
1. `"websocket_message"` (string)
2. url (string) — the WebSocket URL
3. message (string) — the message content
4. binary (boolean) — whether the message is binary-encoded
