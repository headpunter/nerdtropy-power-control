# websocket_failure Event

Fires when a WebSocket connection attempt fails (from `http.websocketAsync`).

## Return Values
1. `"websocket_failure"` (string)
2. url (string) — the requested URL
3. err (string) — error description
